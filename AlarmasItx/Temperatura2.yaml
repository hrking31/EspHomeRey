esphome:
  name: temperatura-sala2

esp8266:
  board: d1_mini_lite

logger:
  # level: DEBUG
  level: WARN

ota:
  - platform: esphome
    password: !secret ota_password

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  min_auth_mode: WPA2
  reboot_timeout: 0s

  on_connect:
    then:
      - lambda: |-
          if (id(sensor_ok) && !std::isnan(id(temp_dht).state)) {
            id(temp_to_send) = id(temp_dht).state;
            id(hum_to_send) = id(hum_dht).state;
            id(send_temp_hum).execute();
          }

  ap:
    ssid: "Temperatura sala_2"
    password: !secret ap_password
    ap_timeout: 90s

captive_portal:

  # ////////////////////////////////////////////////////////////////////////

# -------- HTTP --------
http_request:
  useragent: esphome/d1mini
  timeout: 3s
  verify_ssl: false
  id: http_request_sala2

# -------- VARIABLES --------
globals:
  - id: last_temp_sent_to_function
    type: float
    initial_value: "NAN"

  - id: temp_to_send
    type: float

  - id: hum_to_send
    type: float

  - id: sensor_ok
    type: bool
    initial_value: "false"

  - id: firebase_url_secret
    type: std::string
    initial_value: !secret firebase_sala3_grafica

#---------- LED ESTADO WIFI -----------
output:
  - platform: gpio
    pin: D4
    id: wifi_led

light:
  - platform: binary
    output: wifi_led
    id: wifi_led_light

# -------- TIME --------
time:
  - platform: sntp
    id: esptime
    timezone: "America/Bogota"

# -------- DHT11 --------
sensor:
  - platform: dht
    pin: D1
    model: DHT11
    update_interval: 30s
    temperature:
      id: temp_dht
      filters:
        - filter_out: NAN
        - delta: 0.1
      on_value:
        then:
          - lambda: |-
              id(sensor_ok) = true;
              id(temp_to_send) = x;
              id(hum_to_send) = id(hum_dht).state;

              if (std::isnan(id(last_temp_sent_to_function)) || 
                  std::abs(x - id(last_temp_sent_to_function)) >= 0.3) {
                  id(send_temp_hum).execute();
                id(last_temp_sent_to_function) = x;
              }
    humidity:
      id: hum_dht
      filters:
        - filter_out: NAN

# -------- SCRIPT ENVÍO A FIREBASE TEMP Y HUM --------
script:
  - id: send_temp_hum
    mode: restart 
    then:
      - if:
          condition:
            wifi.connected:
          then:
            - http_request.send:
                id: http_request_sala2
                method: PATCH
                url: !secret firebase_sala2_TempHum
                request_headers:
                  Content-Type: application/json
                body: !lambda |-
                  char buffer[64];

                  if (std::isnan(id(temp_to_send))) {
                    sprintf(buffer, "{\"temperatura\": \"null\", \"humedad\": \"null\"}");
                  } else {
                    sprintf(
                      buffer,
                      "{\"temperatura\": %.2f, \"humedad\": %.2f}",
                      id(temp_to_send),
                      id(hum_to_send)
                    );
                  }

                  return (std::string) buffer;


  # -------- LED INTELIGENTE ESTADO WIFI --------
interval:
  - interval: 1000ms
    then:
      - if:
          condition:
            wifi.ap_active
          then:
            - light.toggle: wifi_led_light
          else:
            - if:
                condition:
                  wifi.connected
                then:
                  - light.turn_on: wifi_led_light
                else:
                  - light.turn_off: wifi_led_light


  # -------- ENVÍO A FIREBASE HEARTBEAT (SIGUE VIVO) --------
  - interval: 60s
    then:
      - if:
          condition:
            wifi.connected:
          then:
            - http_request.send:
                id: http_request_sala2
                method: PATCH
                url: !secret firebase_sala2_heartbeat
                request_headers:
                  Content-Type: application/json
                body: '{"timestamp":{".sv":"timestamp"}}'

  # -------- GRAFICA --------
  - interval: 60s
    then:
      - delay: 15s
      - if:
          condition:
            lambda: |-
              return wifi::global_wifi_component->is_connected() &&
                     id(sensor_ok) &&
                     !std::isnan(id(temp_dht).state) &&
                     id(esptime).now().is_valid();
          then:
            - http_request.post:
                id: http_request_sala2
                url: !lambda |-
                  auto now = id(esptime).now();

                  char url_buffer[160];

                  sprintf(
                    url_buffer,
                    "%s%04d-%02d-%02d/%02d.json",
                    id(firebase_url_secret).c_str(),
                    now.year,
                    now.month,
                    now.day_of_month,
                    now.hour
                  );

                  return std::string(url_buffer);
                request_headers:
                  Content-Type: application/json
                body: !lambda |-
                  float t = id(temp_dht).state;
                  time_t now = id(esptime).now().timestamp;
                  unsigned long long ts_ms = (unsigned long long) now * 1000ULL;

                  char body[64];
                  snprintf(
                    body,
                    sizeof(body),
                    "{\"t\": %.2f, \"ts\": %llu}",
                    t,
                    ts_ms
                  );

                  return std::string(body);

                capture_response: false
